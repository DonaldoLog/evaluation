<div id="app">
    <impresion-component answers-open-initial="{{ $answersOpen }}"
    answers-tutorias-open-initial="{{ $answersTutoriasOpen }}"
    careers-tutoria-initial="{{ $careersTutoria }}"
    careers-initial="{{ $careers }}"
    teacher-initial="{{ $teacher }}"
    answers-initial="{{ $answers }}"
    answerstutorias-initial="{{ $answersTutorias }}"
    groups-initial="{{ $groups }}"
    groups-tutoria-initial="{{ $groupsTutoria }}"
    total-initial="{{ $total }}"
    total2-initial="{{ $total2 }}"
    questionsgraph-initial="{{ $questionsGraph }}"
    questionsgraphtuto-initial="{{ $questionsGraphTuto }}"
    ></impresion-component>
</div>
    <script src="./../../../../js/app.js?val=2021"></script>
    <script> console.log('Hi!'); </script>
